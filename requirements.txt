# Core dependencies for Check Point VM Automation
netmiko>=4.2.0            # Network device SSH library with Check Point support
paramiko>=2.11.0          # SSH client library (for compatibility)
pyyaml>=6.0               # YAML configuration parsing
dataclasses>=0.6          # Backport for Python < 3.7 (if needed)
typing-extensions>=4.0.0  # Extended typing support

# Optional automation backends
nornir>=3.3.0             # Nornir automation framework
nornir-paramiko>=0.1.0    # Paramiko plugin for Nornir
ansible>=6.0.0            # Ansible automation platform

# Development and testing dependencies
pytest>=7.0.0             # Testing framework
pytest-cov>=4.0.0         # Coverage reporting
pytest-mock>=3.8.0        # Mocking support
black>=22.0.0             # Code formatting
flake8>=5.0.0             # Linting
mypy>=0.991               # Type checking

# Documentation
sphinx>=5.0.0             # Documentation generation
sphinx-rtd-theme>=1.0.0   # Read the Docs theme